apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: microservices-demo
  labels:
    app: product-service
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3001
    name: http
